=== Evaluating Large Language Models Trained on Code.pdf ===
- thermore, we ﬁnd that repeated sampling from the
- Scalable sequence prediction models (Graves, 2014;
- We begin by deﬁning thepass@k metric, and
- the pass@k metric, where k code samples are generated
- may be tempted to estimate pass@k with 1−(1− ˆp)k where
- tokens, using the Adam optimizer withβ1 = 0.9,β2 = 0.95,
- GPT, we base our code lexer on the GPT-3 text tokenizer.
- that of natural text, this tokenizer is not very effective for
- tokens for representing whitespace runs of different lengths.
- To compute pass@k, we assemble each HumanEval prob-
- tokens from Codex until we encounter one of the following
- stop sequences: ‘\nclass’, ‘\ndef’, ‘\n#’, ‘\nif’, or
- sampling (Holtzman et al., 2020) with topp = 0.95 for all
- When evaluating pass@k, it is important to optimize sam-
- pling temperature for the particular value ofk.
- we plot pass@k against the number of samples k and the
- Pass@k can also be interpreted as the result of evaluating
- that choosing the sample with the highest mean token log
- In the top panel, we plot pass@k against the number of
- samples (k) for various temperature settings.

=== Program Synthesis with Large Language Models.pdf ===
- Such models emit code in ‘token-space’, and so it is not
- prompt, the identity of examples in prompt, sampling technique, etc.
- tasks that require nontrivial external knowledge, such as the deﬁnition of particular notable integer sequences.
- sequences, and 2% center around the use of other data structures.
- model contains 2.97B documents, which were tokenized into 2.81T BPE tokens with a vocabulary of 32K tokens using
- containing 18.7B BPE tokens out of the pre-training data.
- quality like token accuracy or BLEU (see Section 4.7 for more about this).
- problem in the test dataset, we use temperature sampling (with temperature 0.5) to generate 80 samples of code and then
- We use greedy decoding (temperature set to 0.0) to generate a single approximate most likely generation, and
- prompts produce many samples that do not ﬁt with the 512 token context window (even with a context window of 1024
- tokens, this failure mode is still pronounced).

=== Retrieval-Augmented Generation.pdf ===
- explore a general-purpose ﬁne-tuning recipe for retrieval-augmented generation
- vector index of Wikipedia, accessed with a pre-trained neural retriever.
- pare two RAG formulations, one which conditions on the same retrieved passages
- across the whole generated sequence, and another which can use different passages
- outperforming parametric seq2seq models and task-speciﬁc retrieve-and-extract
- memory with non-parametric (i.e., retrieval-based) memories [20, 26, 48] can address some of these
- combine masked language models [8] with a differentiable retriever, have shown promising results,
- We combine a pre-trained retriever (Query Encoder + Document
- treatz as a latent variable and marginalize over seq2seq predictions given different documents.
- a general-purpose ﬁne-tuning approach which we refer to as retrieval-augmented generation (RAG).
- retriever (Dense Passage Retriever [26], henceforth DPR) provides latent documents conditioned on
- We marginalize the latent documents with a top-K approximation,
- either on a per-output basis (assuming the same document is responsible for all tokens) or a per-token
- basis (where different documents are responsible for different tokens).
- can be ﬁne-tuned on any seq2seq task, whereby both the generator and retriever are jointly learned.
- state-of-the-art pipeline models which use strong retrieval supervision.
- We explore RAG models, which use the input sequencex to retrieve text documentsz and use them
- as additional context when generating the target sequence y.
- leverage two components: (i) a retriever pη(z|x) with parametersη that returns (top-K truncated)
- byθ that generates a current token based on a context of the previousi− 1 tokensy1:i−1, the original

